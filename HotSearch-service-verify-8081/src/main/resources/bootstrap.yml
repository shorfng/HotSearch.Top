# 端口号
server:
  port: 8081

spring:
  application:
    name: hotsearch-service-verify
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: Dev

  #Nacos 配置
  cloud:
    nacos:
      discovery:
        ################ 配置nacos server地址
        server-addr: 192.168.31.130:8848
        namespace: f41de296-29a5-49f3-8fc7-720652f03726 # 命名空间
      config:
        server-addr: 192.168.31.130:8848
        namespace: f41de296-29a5-49f3-8fc7-720652f03726 # 命名空间
        group: PROJECT_GROUP # 分组
        dataid: hotsearch-service-verify-${spring.profiles.active}.yaml
        file-extension: yaml #扩展名

#management:
#    endpoints:
#        web:
#            exposure:
#                include: "*"

dubbo:
  consumer:
    timeout: 5000
  cloud:
    #
    subscribed-services: hotsearch-service-email
